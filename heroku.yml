setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile
    celery: Dockerfile
release:
  image: web
  command:
    - python manage.py collectstatic
run:
  web:
    command:
      - daphne -b 0.0.0.0 -p $PORT backend.asgi:application
    image: web
  celery:
    command:
      - celery worker --app=backend.celery
    image: celery
