setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile
release:
  image: web
  command: python manage.py migrate
run:
  web: python manage.py runsever

