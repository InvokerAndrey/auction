setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile
release:
  command:
    - python manage.py migrate
    image: web
run:
  web:
    - python manage.py runsever

